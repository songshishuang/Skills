# 产品规划 (Product Planning)

本目录沉淀 **{{PROJECT_NAME}}** 的产品架构、规划图、链路设计、答辩 / 评审材料。

## 与其它目录的边界

| | 本目录 | 邻近目录 |
|---|---|---|
| `product-planning/` 是 **我们的产品规划** | `market-research/` 是 **外部世界的信息** |
| `product-planning/` 是 **静态架构图** | `roadmap.md` 是 **滚动的时间线** |
| `product-planning/` 是 **战略层** | `prd/` 是 **战术层** |

## 推荐子文件

- `产品架构图.{html,svg,png}` — 一图说清楚产品全貌
- `场景链路图.{html,svg,png}` — 关键业务场景的端到端链路
- `技术架构图.{html,svg,png}` — 给技术评审看的底层架构
- `{发布会演讲材料}.{html,pdf}` — 对外答辩 / 评审 / 路演

## 当前内容

⚠️ 目录刚初始化。建议第一份产物：**产品架构图**——能让所有团队成员对齐"我们到底在做什么"。

## 沉淀准则

- **图比字重要**——架构图应该 80% 看图就懂，文字只做点睛
- **保留多版本**——v1 → v2 → v3 都保留，便于看演进
- **HTML 优于 PNG**——可交互、可缩放、可文本搜索
