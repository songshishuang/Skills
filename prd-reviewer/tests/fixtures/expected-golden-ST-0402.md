# expected · golden-ST-0402-告警历史(正样例预期输出)

> 用途:验证门禁不误杀合格 spec(防 skill 修改后检查项变得过严)。

## 第一关 standard lint · 预期

| 检查项 | 预期结论 |
|---|---|
| 1 frontmatter | pass(7 字段齐全) |
| 2 AC 可测性 | pass(AC1-4 全 GWT,无不可测词) |
| 3 「不做」边界 | pass(3 项实质边界,相邻功能注明所属) |
| 4 技术细节 | pass(字段全业务级,无 PK/FK/类型) |
| 5 行数/重复 | pass(<100 行;R1/R2 按编号引用未复制正文) |
| 6 依赖 | pass(ST-0401 在 overview 索引中存在,无环) |
| 7 路径可解析 | 豁免仅限文件存在性(fixture 环境无真实 PRD/原型文件,标 N/A);**路径层级结构必须与 00-overview 基准一致(source=`../` · prototype=`../../`),结构不一致不在豁免范围内,应报 blocker** |

**预期总结论:READY**

## 第二关 对抗评审 · 预期

**预期判定:SPEC-CLEAR**,允许 ≤5 个低风险猜测点(如列表列序、详情 drawer 宽度等视觉细节——原型职责),不应出现高风险猜测。

## 回归通过标准

第一关 READY(任何 blocker = 门禁过严或样例腐化,需排查);第二关 SPEC-CLEAR 且无高风险猜测(中/低风险猜测 ≤5 个为正常——判定规则只在「高风险 ≥1 或总数 >5」时 AMBIGUOUS)。

## 盲测实绩

- **2026-06-12 · 独立盲测(隔离实例,未见本预期文件)**:第一关 **READY**(7 pass / 0 warning / 0 blocker;第 7 项按修正后的结构基准条款判定 pass——source `../` 与 prototype `../../` 均与 00-overview 一致);第二关 **SPEC-CLEAR**(猜测点 5 个未超限,高风险 0)。
- 附注:盲测在本 golden 样例中挖出 1 个**中风险**猜测点——背景(L17)陈述「供应商追溯」诉求但 AC/字段/筛选均未覆盖供应商维度。判定不受影响(规则内),且恰好证明第二关对合格样例仍有挖掘灵敏度;该点保留在样例中作为「对抗评审有牙齿」的活证据,不修——修掉它反而会让 golden 失去这个测灵敏度的探针。
