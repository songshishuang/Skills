---
slice: for-coding
source_prd: docs/prd/V{X.Y}/PRD-V{X.Y}.md
generated_at: {YYYY-MM-DD}
note: 本切片自动从全本 PRD 抽取 · AI 编码 agent 专用 · 不可手工编辑 · 修改请改全本
---

# {产品名} V{X.Y} · 编码切片

> **For AI Coding Agents Only.** 业务方 / QA / BI / SRE / 运营请读全本：[`PRD-V{X.Y}.md`](../PRD-V{X.Y}.md)
>
> 本切片仅含**编码可执行**的内容 —— 业务对象、页面设计、AI 模块、事件 schema、性能门槛、ADR。
> 砍掉了：北极星/KR、用户痛点、灰度计划、回滚预案、成本预算、合规备案、评测集设计、验收清单、排期 —— 这些与编码 0 相关，需要请读全本。

---

## 一、本期做什么（Summary）

{原 §1 版本说明 · 3-5 bullets · 全保留}

1. {核心变化 1}
2. {核心变化 2}
3. {核心变化 3}

---

## 二、上下文（Context · 极简）

### 业务背景
{原 §2.1 · 1-2 段 · 全保留 —— agent 取业务命名 + 文案语调}

### 用户角色 + 权限矩阵
| 角色 | 可做 | 不可做 |
|---|---|---|
| {角色 A} | {能力清单} | {禁区} |

> {从全本 §2.2 提取角色名 + 权限两列 · 砍掉"主要诉求"散文}

### 非目标（No-gos）
{原 §2.4 · 全保留 —— **关键** · 防止 agent 过度实现}

- ❌ {不做什么}

### 端到端流程图
{原 §2.5 · 全保留 mermaid}

---

## 三、需求范围（按 P0/P1/P2）

{原 §3.1 表格全保留 · 砍掉 §3.2 非功能性需求（部分挪到下方 §5 性能门槛）}

| 序号 | 功能 | 类型 | 优先级 | 需求摘要 |
|---|---|---|---|---|

---

## 四、Design（编码核心 · 全保留）

### 4.1 业务对象关系
{原 §4.1 5 子段全保留 —— 总览 + erDiagram + 状态机 + 业务规则 + 对象-页面映射}

### 4.2 页面功能详细设计
{原 §4.2 每页 7 段全保留}

### 4.3 AI 模块特化（仅对外生成式 AI · 砍 4.3.5 兜底文案外的成本估算）
{原 §4.3 保留 6 小节 · 砍掉「单次调用成本估算」「月度预算估算」（运营关心，非编码项）}

---

## 五、性能门槛与可观测性（最小集）

> 全本 §3.2 非功能性需求 + §4.3.6 可观测性的**编码必需子集**。
> 砍掉 SLA / 数据备份 / 浏览器兼容 / 合规备案 / PII 脱敏策略 / 数据保留期。

### 性能（进 timeout / retry 配置）
- AI 响应 P99 ≤ {阈值 ms}
- 页面加载 P99 ≤ {阈值 ms}
- 并发 {QPS}

### Trace 必带字段（写日志 / 上报时必含）
`trace_id` / `model` / `prompt_version` / `latency_ms` / `token_in` / `token_out` / `confidence` / `kb_hits` / `fallback_reason`

---

## 六、事件 Schema（编码必需 · 3 列）

> 全本 §6.1 埋点表的**编码子集** —— 仅事件 ID + 上报时机 + 扩展字段。
> 砍掉：事件描述（注释里写）/ 看板规划 / 告警阈值 / 页面路径名称 / 备注。

| 事件 ID | 上报时机 | 扩展字段（含公共 ✓） |
|---|---|---|
| `{event_a}` | {时机} | {字段清单} + 公共 ✓ |
| `ai_response_show` | 模型输出完成 UI 渲染 | trace_id / model / skill_id / latency_ms / token_in / token_out / confidence / kb_hits + 公共 ✓ |
| `ai_response_adopt` | 点击「确认/发送」（无编辑） | trace_id / no_edit=true + 公共 ✓ |
| `ai_response_reject` | 关闭 + 重写 | trace_id / reject_reason + 公共 ✓ |
| `ai_fallback_trigger` | 兜底分支命中 | trace_id / reason + 公共 ✓ |
| `llm_call_cost` | LLM 调用完成 | trace_id / model / token_in / token_out / cost_estimate + 公共 ✓ |

> **公共字段 ✓**（每事件必带）：`event_id` / `timestamp` / `user_id` / `user_role` / `project_id` / `session_id` / `client` / `env`

---

## 七、ADR 关键决策（避免 agent second-guess · 全保留）

{原 §7.2 全保留 —— 直接给 agent "为什么这么选" 的答案}

| # | 决策 | 选择 | 理由 |
|---|---|---|---|
| 1 | {模型选择} | {选定} | {三角理由} |

---

## 编码 agent 使用提示

- 本切片**只能读 / 不能写**。需要修改任何业务规则、字段、状态机定义 → 改全本 PRD，重新生成切片
- 看到本切片**没出现**的内容（如灰度阈值、成本预算、合规备案、评测集设计），不要尝试实现 —— 那些是其他角色的责任
- 看到本切片里**仍有不确定**的内容 → 读全本对应章节 / 查 ADR / 找 PM 而不是自己拍板
- 切片内**字段 / 事件 / 状态名**与代码保持完全一致；如发现切片字段与代码模型不一致，**以代码为准 + 提 issue 让 PM 改 PRD**
